import type { VenueType } from '@/shared/api/markers'

export const venueLabels: Record<VenueType, string> = {
    metro: 'Metro',
    hospital: 'Hospital',
    mall: 'Mall',
    railway_station: 'Railway station',
    school: 'School',
    other: 'Other',
}
